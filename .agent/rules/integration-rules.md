---
trigger: always_on
---

INT-001: When some data mapping between two different format is required, use a dedicated function to handle the data mapping.
INT-002: When some data mapping between two or more different data concetps is required, use a dedicated function to handle the data mapping.