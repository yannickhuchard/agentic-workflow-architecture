---
trigger: always_on
---

DA-001: always follow the data model to ensure the coherence between UI components, APIs, functions, database, files 
DA-002: always follow the data model to build API
DA-003: always follow the data model to build functions
DA-004: always follow th data model to build UI components 
DA-005: always perform data modeling first before coding features.
DA-006: after the data model updated and the code implemented from the feature, always verify the coherence and integrity of the defined class, types, structures with the UI web components, API, messaging, functions, databases, files, objects within the current change.
DA-007: do not create mockup data nor sample dat unless it is explicitly requested by the user. You MUST always rely upon data from the database or static schema that are fetched from the API if it is based upon API architecture.